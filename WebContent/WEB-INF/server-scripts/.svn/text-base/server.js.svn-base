//Import core Java packages
importPackage(java.lang);

//Import core SAJE packages
importPackage(net.saje.js.adapters);

var SAJE = {};

//Build the SAJE object
SAJE.db = PersistenceAdapter.getInstance();

SAJE.session = SessionAdapter.getInstance();

SAJE.authentication = AuthenticationAdapter.getInstance();

SAJE.utils = JavascriptSupportAdapter.getInstance();

SAJE.form = FormHelperAdapter.getInstance();
